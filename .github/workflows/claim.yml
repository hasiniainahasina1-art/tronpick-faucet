name: Claim Tronpick Faucet

on:
  schedule:
    - cron: '0 */6 * * *'   # toutes les 6 heures (modifiable)
  workflow_dispatch:         # déclenchement manuel

jobs:
  claim:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Récupérer le code source
        uses: actions/checkout@v4

      - name: 📦 Installer les dépendances système (xvfb)
        run: |
          sudo apt-get update
          sudo apt-get install -y xvfb

      - name: 🟢 Installer Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 📦 Installer les dépendances Node.js
        run: npm install

      - name: 🤖 Exécuter le script avec xvfb (navigateur furtif)
        env:
          TRONPICK_EMAIL: ${{ secrets.TRONPICK_EMAIL }}
          TRONPICK_PASSWORD: ${{ secrets.TRONPICK_PASSWORD }}
          PROXY_USERNAME: ${{ secrets.PROXY_USERNAME }}
          PROXY_PASSWORD: ${{ secrets.PROXY_PASSWORD }}
        run: xvfb-run --auto-servernum --server-args="-screen 0 1280x720x24" node script.js

      - name: 📝 Commiter et pousser le nouveau statut
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add public/status.json
          git diff --quiet && git diff --staged --quiet || (git commit -m "🔄 Mise à jour du statut faucet [skip ci]" && git push)
